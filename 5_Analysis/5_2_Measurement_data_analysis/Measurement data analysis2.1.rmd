---
title: "实证数据分析"
author: "why"
date: "2024.9.5"
output: html_document
---
```{r}
# 数据处理完整思路
# 1：载入需要的数据。原始数据中除抑郁问卷外，还包括焦虑问卷，因此只载入测量抑郁的问卷，及人口学信息。具体为DSRSC、CDI、PHQ-9、和DASS-21中测量抑郁的题目。
# 2：转换数据为数值型。
# 3：处理缺失值（实际没有缺失值）。
# 4：原始数据-1，用以方便计算检出率。原始数据的范围与量表正常的数据范围是不一致的。例如：DSRSC量表的计分范围正常应该为0-2，检出率为≥15。但咱们收到的原始数据计分范围就是1-3，也就是所有数值都+了1。因此-1后才能计算正确的检出率。
# 5：描述统计，原始数据的年龄范围显示为0-29岁，因此参考李医生的建议，筛除了7岁以下和18岁以上的人。
# 计算了1、性别比例。2、年龄范围。
# 6：数据质量检查。首先，使用原始数据进行了相关分析，用以检查数据。
# 发现问题：1、DSRSC问卷内相关有正有负。2、CDI问卷内相关有正有负。因此，数据应该进行反向计分，与李医生讨论过确实需要进行反向计分。
# 7：反向计分，以及反向计分后的相关分析。

# 上为数据预处理，下面开始正式检验。

# 8: 我们对问卷内部的题目进行了合并，并且在跨问卷中认为一些题目是测量同一症状的。
# 因此，这些特定题目之间的相关，应该高于其他题目的相关。
# 对此，我们对特定题目的相关系数和其他题目的相关系数进行了Z检验。
# 大部分特定题目的相关系数是高于其他的，详细结果见8及8.1。


# 9：我们认为儿童青少年不同问卷之间的检出率应该有差异。因此进行了检出率计算。
# 结果显示：CDI的检出率为49.25，DSRSC的检出率为52.12.PHQ-9的检出率为53.46.


# 10：我们想要探究不同症状之间可以形成多少个cluster，因此进行了Exploratory Graph Analysis
# 由于不同问卷之间的题目，可能测量的是同一个症状，因此我们对其进行了合并，以取均值的形式，合并了不同问卷之间的题目。


# 载入必要的包
library(here)
library(bruceR)
library(tidyverse)
library(openxlsx)
library(ggcorrplot)

# 1. 载入数据
raw_data <- bruceR::import(here::here("data", "Rawdata1.xlsx"))

selected_data <- raw_data %>%
  select(ID, Gender, Age, 
         DSRSC1:DSRSC18, 
         PHQ1:PHQ9, 
         DASS3, DASS5, DASS10, DASS13, DASS16, DASS17, DASS21, 
         CDI1:CDI27)

# 2.转换数据类型，性别从“男”转换为1，“女”转换为2，其他变量转换为数值型
selected_data_numeric <- selected_data %>%
  mutate(Gender = factor(Gender, levels = c("男", "女"), labels = c(1, 2))) %>%
  mutate(across(matches("Gender|Age|DSRSC|PHQ|DASS|CDI"), as.numeric))


#3.处理缺失值
clean_data <- selected_data_numeric %>%
  drop_na()

#4.原始数据-1，用以方便计算检出率
transformed_data <- clean_data %>%
  mutate(across(matches("DSRSC|PHQ|DASS|CDI"), ~ .x - 1))

#5.筛选出年龄在7岁以上且不超过18岁的数据
filtered_data <- transformed_data %>%
  filter(Age >= 7 & Age <= 18)

# 描述统计
# 计算性别比例
gender_proportion <- filtered_data %>%
  summarise(
    male = sum(Gender == 1, na.rm = TRUE),
    female = sum(Gender == 2, na.rm = TRUE),
    total = n(),
    male_proportion = male / total * 100,
    female_proportion = female / total * 100
  )
# 打印性别比例
print(gender_proportion)

# 计算年龄的范围
age_range <- filtered_data %>%
  summarise(
    min_age = min(Age, na.rm = TRUE),
    max_age = max(Age, na.rm = TRUE)
  )

# 打印年龄范围
print(age_range)

# 检查并创建输出目录
output_dir <- here::here("output")
if (!dir.exists(output_dir)) {
  dir.create(output_dir)
}
```

```{r}
# 6. 数据质量检查，反向计分前的相关分析
correlation_matrix <- filtered_data %>%
  select(matches("DSRSC|PHQ|DASS|CDI")) %>%
  cor(method = "spearman")  # 使用Spearman相关系数

# 导出
output_file <- here::here("output", "correlation_matrix_spearman.xlsx")
write.xlsx(correlation_matrix, output_file)

# 可视化
p <- ggcorrplot(correlation_matrix, lab = TRUE, 
                method = "circle", 
                outline.color = "white", 
                colors = c("blue", "white", "red"), 
                ggtheme = ggplot2::theme_minimal(base_family = "sans"), 
                title = "未反向计分的斯皮尔曼相关系数") +  # 修改标题
  theme_minimal(base_family = "sans") + 
  theme(
    text = element_text(size = 20, color = "black"), 
    axis.text.x = element_text(angle = 90, hjust = 1, vjust = 0.5, size = 14), 
    axis.text.y = element_text(size = 14), 
    axis.title = element_text(size = 22), 
    plot.background = element_rect(fill = "gray90"), 
    panel.grid.major = element_blank(), 
    panel.grid.minor = element_blank(), 
    panel.background = element_rect(fill = "gray90", color = NA), 
    legend.position = "bottom", 
    legend.key.width = unit(4, "cm"), 
    legend.key.height = unit(1, "cm")
  ) +
  scale_size_continuous(range = c(5, 12)) +
  labs(x = "", y = "")

# 保存图片
ggsave(here::here("output", "correlation_heatmap_spearman.png"),  # 修改文件名
       plot = p, width = 32, height = 28, dpi = 300)

```


```{r}
# 7.进行反向计分
# CDI 反向计分项目
CDI_reverse_items <- c("CDI2", "CDI5", "CDI7", "CDI8", "CDI10", 
                       "CDI11", "CDI13", "CDI15", "CDI16", 
                       "CDI18", "CDI21", "CDI24", "CDI25")

# DSRSC 反向计分项目
DSRSC_reverse_items <- c("DSRSC3", "DSRSC5", "DSRSC6", "DSRSC10", 
                         "DSRSC14", "DSRSC15", "DSRSC17", "DSRSC18")

# 对CDI和DSRSC项目进行反向计分
reversed_data <- filtered_data %>%
  mutate(across(all_of(CDI_reverse_items), ~ 2 - .)) %>%  # 对于0-2范围，反向计分为2减去原分数
  mutate(across(all_of(DSRSC_reverse_items), ~ 2 - .))

# 计算反向计分后的相关性
correlation_matrix_reversed <- reversed_data %>%
  select(matches("DSRSC|PHQ|DASS|CDI")) %>%
  cor(method = "spearman")  # 使用Spearman相关系数

# 导出
output_file_reversed <- here::here("output", "correlation_matrix_reversed_spearman.xlsx")
write.xlsx(correlation_matrix_reversed, output_file_reversed)

# 可视化
p_reversed <- ggcorrplot(correlation_matrix_reversed, lab = TRUE, 
                         method = "circle", 
                         outline.color = "white", 
                         colors = c("blue", "white", "red"), 
                         ggtheme = ggplot2::theme_minimal(base_family = "sans"), 
                         title = "反向计分后的斯皮尔曼相关系数") +  # 修改标题
  theme_minimal(base_family = "sans") + 
  theme(
    text = element_text(size = 20, color = "black"), 
    axis.text.x = element_text(angle = 90, hjust = 1, vjust = 0.5, size = 14), 
    axis.text.y = element_text(size = 14), 
    axis.title = element_text(size = 22), 
    plot.background = element_rect(fill = "gray90"), 
    panel.grid.major = element_blank(), 
    panel.grid.minor = element_blank(), 
    panel.background = element_rect(fill = "gray90", color = NA), 
    legend.position = "bottom", 
    legend.key.width = unit(4, "cm"), 
    legend.key.height = unit(1, "cm")
  ) +
  scale_size_continuous(range = c(5, 12)) +
  labs(x = "", y = "")

# 保存图片
ggsave(here::here("output", "correlation_heatmap_reversed_spearman.png"),  # 修改文件名
       plot = p_reversed, width = 32, height = 28, dpi = 300)
```


```{r}
# 8.检验问卷内部合并的症状的相关系数是否大于其他的。
# 使用斯皮尔曼相关系数
# DSRSC合并了2和14、10和12。
# CDIQ3Q7Q24合并 Q15Q23合并。
# 做了DSRSC和CDI每个合并的条目和其他条目（即该量表去除此条目以外的条目）的Z检验。

# 定义 z_transform 函数
z_transform <- function(r) {
  return(0.5 * log((1 + r) / (1 - r)))  # Fisher's Z transformation
}

# 自定义函数，用于比较特定项目与其他项目的相关系数
compare_specific_projects <- function(cor_matrix, project1, project2, scale_prefix) {
  # 提取特定项目的相关系数
  r_value <- cor_matrix[project1, project2]
  
  # 获取该量表的相关系数，排除特定项目
  other_r_values <- cor_matrix[grep(paste0("^", scale_prefix), rownames(cor_matrix)), 
                                grep(paste0("^", scale_prefix), colnames(cor_matrix))]
  
  # 去除特定项目
  other_r_values <- other_r_values[-which(rownames(other_r_values) %in% c(project1, project2)), 
                                    -which(colnames(other_r_values) %in% c(project1, project2))]
  
  # 将其他相关系数展开为向量
  other_r_values_vector <- as.vector(other_r_values)
  
  # 计算其他相关系数的均值和标准差
  mean_other_r <- mean(other_r_values_vector, na.rm = TRUE)
  
  # 进行z检验
  z_value <- z_transform(r_value)
  z_mean_other <- z_transform(mean_other_r)
  
  # 计算标准误差
  se <- 1 / sqrt(nrow(filtered_data) - 3)  # 样本大小
  
  # 计算z分数和p值
  z_score <- (z_value - z_mean_other) / se
  p_value <- 2 * (1 - pnorm(abs(z_score)))
  
  # 返回结果
  result <- data.frame(
    specific_project_r = r_value,
    mean_other_r = mean_other_r,
    z_score = z_score,
    p_value = round(p_value, 3)  # 保留三位小数
  )
  
  return(result)
}

# 比较 DSRSC 特定项目
comparison_result_2_14 <- compare_specific_projects(correlation_matrix_reversed, "DSRSC2", "DSRSC14", "DSRSC")
comparison_result_10_12 <- compare_specific_projects(correlation_matrix_reversed, "DSRSC10", "DSRSC12", "DSRSC")

# 合并 DSRSC 结果
combined_result <- rbind(comparison_result_2_14, comparison_result_10_12)
rownames(combined_result) <- c("DSRSC2与DSRSC14", "DSRSC10与DSRSC12")
print(combined_result)

# 比较 CDI 特定项目
comparison_result_3_24 <- compare_specific_projects(correlation_matrix_reversed, "CDI3", "CDI24", "CDI")
comparison_result_7_24 <- compare_specific_projects(correlation_matrix_reversed, "CDI7", "CDI24", "CDI")
comparison_result_3_7 <- compare_specific_projects(correlation_matrix_reversed, "CDI3", "CDI7", "CDI")
comparison_result_15_23 <- compare_specific_projects(correlation_matrix_reversed, "CDI15", "CDI23", "CDI")

# 合并 CDI 结果
combined_result_CDI <- rbind(comparison_result_3_24, 
                              comparison_result_7_24, 
                              comparison_result_3_7, 
                              comparison_result_15_23)
rownames(combined_result_CDI) <- c("CDI3与CDI24", "CDI7与CDI24", "CDI3与CDI7", "CDI15与CDI23")
print(combined_result_CDI)



```


```{r}
# 8.1 检验问卷间测量同一个症状的相关系数是否大于其他的。
# 测量相同症状的有 1.DSRSC16 PHQ2 2.CDI1 DSRSC17 3.CDI4 DASS3
# 4.CDI11 DSRSC18 5.CDI20 DSRSC15 6.DSRSC11 PHQ6 7.CDI2 DSRSC1 DASS10
# 8.CDI3 7 24  DASS17 9.DSRSC7 DASS5 10.DSRSC10 12 PHQ1 11.CDI17 PHQ4 12.CDI18 PHQ5
# 13. CDI16 PHQ3 14.CDI12 DSRSC4 15.CDI10 DSRSC3 16.CDI9 PHQ9
# 做了四个量表每个合并的条目的相关系数和其他条目（四个量表去除此条目以外的条目）平均相关系数的Z检验。


# 自定义函数，用于比较跨量表项目的相关系数
compare_cross_scale_projects <- function(cor_matrix, project1, project2) {
  # 提取特定项目的相关系数
  r_value <- cor_matrix[project1, project2]
  
  # 获取所有项目的相关系数
  all_r_values <- cor_matrix
  
  # 去除特定项目
  other_r_values <- all_r_values[-which(rownames(all_r_values) %in% c(project1, project2)), 
                                  -which(colnames(all_r_values) %in% c(project1, project2))]
  
  # 将其他相关系数展开为向量
  other_r_values_vector <- as.vector(other_r_values)
  
  # 计算其他相关系数的均值
  mean_other_r <- mean(other_r_values_vector, na.rm = TRUE)
  
  # 进行z检验
  z_value <- z_transform(r_value)
  z_mean_other <- z_transform(mean_other_r)
  
  # 计算标准误差
  se <- 1 / sqrt(nrow(filtered_data) - 3)  # 样本大小
  
  # 计算z分数
  z_score <- (z_value - z_mean_other) / se
  
  # 计算p值
  p_value <- 2 * (1 - pnorm(abs(z_score)))
  
  # 返回结果
  result <- data.frame(
    specific_project_r = r_value,
    mean_other_r = mean_other_r,
    z_score = z_score,
    p_value = round(p_value, 3)  # 保留三位小数
  )
  
  return(result)
}

# 需要比较的项目列表
project_pairs <- list(
  c("DSRSC16", "PHQ2"), c("CDI1", "DSRSC17"), c("CDI4", "DASS3"), 
  c("CDI11", "DSRSC18"), c("CDI20", "DSRSC15"), c("DSRSC11", "PHQ6"),
  c("CDI2", "DSRSC1"), c("CDI2", "DASS10"), c("DSRSC1", "DASS10"),
  c("CDI3", "DASS17"), c("CDI7", "DASS17"), c("CDI24", "DASS17"),
  c("DSRSC7", "DASS5"), c("DSRSC10", "PHQ1"), c("DSRSC12", "PHQ1"),
  c("CDI17", "PHQ4"), c("CDI18", "PHQ5"), c("CDI16", "PHQ3"),
  c("CDI12", "DSRSC4"), c("CDI10", "DSRSC3"), c("CDI9", "PHQ9")
)

# 比较并存储结果
comparison_results <- do.call(rbind, lapply(project_pairs, function(pair) {
  compare_cross_scale_projects(correlation_matrix_reversed, pair[1], pair[2])
}))

# 设置行名为项目对
rownames(comparison_results) <- sapply(project_pairs, function(pair) {
  paste(pair[1], "与", pair[2])
})

# 打印结果
print(comparison_results)



```



```{r}
# 9.计算检出率，并且可视化。

library(ggplot2)
library(here)

# 设置阈值
cutoff_DSRSC <- 15
cutoff_PHQ <- 10
cutoff_CDI <- 20  

# 计算总分
# CDI26题不得分
total_DSRSC_scores <- rowSums(reversed_data[, grep("^DSRSC", names(reversed_data))])
total_PHQ_scores <- rowSums(reversed_data[, grep("^PHQ", names(reversed_data))])
total_CDI_scores <- rowSums(reversed_data[, grep("^CDI", names(reversed_data))]) - reversed_data$CDI26

# 计算超过cutoff的人数
detected_DSRSC <- sum(total_DSRSC_scores >= cutoff_DSRSC)
detected_PHQ <- sum(total_PHQ_scores >= cutoff_PHQ)
detected_CDI <- sum(total_CDI_scores >= cutoff_CDI)

# 计算总样本数量
total_count <- nrow(reversed_data)

# 计算检出率
detection_rate_DSRSC <- (detected_DSRSC / total_count) * 100
detection_rate_PHQ <- (detected_PHQ / total_count) * 100
detection_rate_CDI <- (detected_CDI / total_count) * 100

# 打印结果
cat("DSRSC总分超过cutoff的人数:", detected_DSRSC, "\n")
cat("DSRSC检出率:", detection_rate_DSRSC, "%\n")
cat("PHQ总分超过cutoff的人数:", detected_PHQ, "\n")
cat("PHQ检出率:", detection_rate_PHQ, "%\n")
cat("CDI总分超过cutoff的人数:", detected_CDI, "\n")
cat("CDI检出率:", detection_rate_CDI, "%\n")

# 创建检出率数据框
detection_rates <- data.frame(
  Measure = c("DSRSC", "PHQ-9", "CDI"),
  Detection_Rate = c(detection_rate_DSRSC, detection_rate_PHQ, detection_rate_CDI)
)

# 绘制图形
ggplot(detection_rates, aes(x = Measure, y = Detection_Rate, fill = Measure)) +
  geom_bar(stat = "identity", color = "black") +
  scale_fill_manual(values = c("#1f77b4", "#ff7f0e", "#2ca02c")) + 
  labs(x = "Measure", y = "Detection Rate (%)") +
  theme_minimal(base_size = 15) +
  theme(
    panel.grid.major = element_blank(),
    panel.grid.minor = element_blank(),
    panel.background = element_rect(fill = "white"),
    plot.background = element_rect(fill = "white"),
    plot.title = element_blank(),
    legend.position = "none"
  ) +
  geom_text(aes(label = round(Detection_Rate, 2)), vjust = -0.5)

# 保存图像到figure文件夹
ggsave(here::here("figure", "detection_rates_plot.png"), width = 8, height = 5, dpi = 300)

# 计算特定个体


# PHQ-9检出，但DSRSC和CDI未检出的人
specific_phq_positive <- reversed_data[total_PHQ_scores >= cutoff_PHQ & total_DSRSC_scores < cutoff_DSRSC & total_CDI_scores < cutoff_CDI, ]
cat("在PHQ-9检出，但DSRSC和CDI未检出的人数:", nrow(specific_phq_positive), "\n")
if(nrow(specific_phq_positive) > 0) {
  print(specific_phq_positive)
}

# CDI检出，但PHQ-9和DSRSC未检出的人
specific_cdi_positive <- reversed_data[total_CDI_scores >= cutoff_CDI & total_PHQ_scores < cutoff_PHQ & total_DSRSC_scores < cutoff_DSRSC, ]
cat("在CDI检出，但PHQ-9和DSRSC未检出的人数:", nrow(specific_cdi_positive), "\n")
if(nrow(specific_cdi_positive) > 0) {
  print(specific_cdi_positive)
}

# DSRSC检出，但PHQ-9和CDI未检出的人
specific_dsrsc_positive <- reversed_data[total_DSRSC_scores >= cutoff_DSRSC & total_PHQ_scores < cutoff_PHQ & total_CDI_scores < cutoff_CDI, ]
cat("在DSRSC检出，但PHQ-9和CDI未检出的人数:", nrow(specific_dsrsc_positive), "\n")
if(nrow(specific_dsrsc_positive) > 0) {
  print(specific_dsrsc_positive)
}

```

```{r}
# 10.Exploratory Graph Analysis 
# 将问卷名改成症状名，不合并版本
# 移除不需要的变量（性别、年龄、就诊卡号）
network_data <- reversed_data %>%
  select(starts_with("DSRSC"), starts_with("PHQ"), starts_with("DASS"), starts_with("CDI"))


# 保留原始的 network_data 并创建新数据集
new_1_network_data <- network_data

# 批量修改变量名称
colnames(new_1_network_data) <- c(
  "Sad(CDI1)", "Feeling of hopelessness(CDI2)", "Psychological inferiority(CDI3)", 
  "Anhedonia(CDI4)", "I am bad all the time", "Psychological worry", 
  "Psychological inferiority(CDI7)", "Self-blame/Guilt", "Suicidal ideation(CDI9)", 
  "Cry(CDI10)", "Psychic anxiety(CDI11)", "Reduced socialization(CDI12)", 
  "Indecisiveness", "Negative body perception", "Learning difficulties", 
  "Somnipathy(CDI16)", "Fatigue(CDI17)", "Appetite Change(CDI18)", 
  "Somatic worry", "Feeling of loneliness(CDI20)", "I never have fun at school", 
  "Lack of friends", "Learning difficulties", "Psychological inferiority(CDI24)", 
  "Felt people disliked me", "I never do what I'm told", "I get into fights all the time", 
  "Feeling of hopelessness(DS1)", "Poor sleep(DS2)", "Cry(DS3)", 
  "Reduced socialization(DS4)", "Running away from home", "Gastrointestinal", 
  "Loss of energy(DS7)", "Appetite increase", "Inferiority/Self-confidence", 
  "Interest/ Pleasure loss(DS10)", "Feeling of failure(DS11)", 
  "Interest/ Pleasure loss(DS12)", "I like talking with my family", 
  "Poor sleep(DS14)", "Feeling of loneliness(DS15)", "Low mood(DS16)", 
  "Sad(DS17)", "Psychic anxiety(DS18)", "Interest/ Pleasure loss(PHQ1)", 
  "Low mood(PHQ2)", "Somnipathy(PHQ3)", "Fatigue(PHQ4)", "Appetite Change(PHQ5)", 
  "Feeling of failure(PHQ6)", "Concentration", "Retardation&Agitation", 
  "Suicidal ideation(PHQ9)", "Anhedonia(DA3)", "Loss of energy(DA5)", 
  "Feeling of hopelessness(DA10)", "Blue", "Interest loss", 
  "Psychological inferiority(DA17)", "Feeling of worthlessness"
)


```

```{r}
# 不合并版本的作图
# 加载所需的包
library(EGAnet)

# 1.执行Unique Variable Analysis（唯一变量分析）
unmerge_uva <- UVA(
  data = new_1_network_data,  # 使用你的数据集
  key = colnames(new_1_network_data)  # 使用变量名作为key
)

# Print results
unmerge_uva

unmerge_uva$keep_remove  # 查看哪些变量需要保留（keep）或删除（remove）

```

```{r}

pdf(here("figure", "EGA_network_plot_unmerge.pdf"), width = 10, height = 8)

# 基于Unique Variable Analysis结果进行EGA分析
unmerge_ega <- EGA(data = unmerge_uva$reduced_data,plot.EGA = TRUE,node.size = 15, label.size = 2)  # 使用排除掉唯一变量后的数据

# 查看EGA分析结果
summary(unmerge_ega)

```





```{r}

# 10.Exploratory Graph Analysis 
# 合并的版本
# 将测量同一症状的题目合并，取均值的形式。
new_2_network_data <- network_data %>%
 
  mutate(
    Sad = rowMeans(select(., CDI1, DSRSC17), na.rm = TRUE), 
    Feeling_of_hopelessness = rowMeans(select(., CDI2, DSRSC1, DASS10), na.rm = TRUE), 
    Psychological_inferiority = rowMeans(select(., CDI3, CDI7, CDI24, DASS17), na.rm = TRUE), 
    Anhedonia = rowMeans(select(., CDI4, DASS3), na.rm = TRUE), 
    Bad_all_the_time = CDI5,
    Psychological_worry = CDI6,
    Self_blame = CDI8,
    Suicidal_ideation = rowMeans(select(., CDI9, PHQ9), na.rm = TRUE), 
    Cry = CDI10,
    Psychic_anxiety = rowMeans(select(., CDI11, DSRSC18), na.rm = TRUE), 
    Reduced_socialization = rowMeans(select(., CDI12, DSRSC4), na.rm = TRUE),
    Indecisiveness = CDI13,
    Negative_body_perception = CDI14,
    Learning_difficulties = rowMeans(select(., CDI15, CDI22, CDI23), na.rm = TRUE),
    Somnipathy = rowMeans(select(., CDI16, PHQ3), na.rm = TRUE), 
    Fatigue = rowMeans(select(., CDI17, PHQ4), na.rm = TRUE), 
    Appetite_change = rowMeans(select(., CDI18, PHQ5), na.rm = TRUE),
    Somatic_worry = CDI19,
    Feeling_of_loneliness = rowMeans(select(., CDI20, DSRSC15), na.rm = TRUE), 
    Never_have_fun_at_school = CDI21,
    Poor_sleep = rowMeans(select(., DSRSC2, DSRSC14), na.rm = TRUE),
    Running_away_from_home = DSRSC5,
    Gastrointestinal = DSRSC6,
    Loss_of_energy = rowMeans(select(., DSRSC7, DASS5), na.rm = TRUE), 
    Appetite_increase = DSRSC8,
    Inferiority_self_confidence = DSRSC9, 
    Interest_pleasure_loss = rowMeans(select(., DSRSC10, DSRSC12, PHQ1), na.rm = TRUE), 
    Feeling_of_failure = rowMeans(select(., DSRSC11, PHQ6), na.rm = TRUE), 
    Like_talking_with_family = DSRSC13,
    Low_mood = rowMeans(select(., DSRSC16, PHQ2), na.rm = TRUE), 
    Concentration = PHQ7,
    Retardation_and_agitation = PHQ8, 
    Blue = DASS13, 
    Interest_loss = DASS16, 
    Feeling_of_worthlessness = DASS21
  ) %>%
  
  # 只保留新变量，去掉原有的变量
  select(Sad, Feeling_of_hopelessness, Psychological_inferiority, Anhedonia, 
         Bad_all_the_time, Psychological_worry, Self_blame, 
         Suicidal_ideation, Cry, Psychic_anxiety, Reduced_socialization, 
         Indecisiveness, Negative_body_perception, Learning_difficulties, 
         Somnipathy, Fatigue, Appetite_change, Somatic_worry, 
         Feeling_of_loneliness, Never_have_fun_at_school, Poor_sleep, 
         Running_away_from_home, Gastrointestinal, Loss_of_energy, 
         Appetite_increase, Inferiority_self_confidence, 
         Interest_pleasure_loss, Feeling_of_failure, 
         Like_talking_with_family, Low_mood, Concentration, 
         Retardation_and_agitation, Blue, Interest_loss, 
         Feeling_of_worthlessness)


```


```{r}
# 合并版本的作图
# 加载所需的包
library(EGAnet)

# 1.执行Unique Variable Analysis（唯一变量分析）
merge_uva <- UVA(
  data = new_2_network_data,  # 使用你的数据集
  key = colnames(new_2_network_data)  # 使用变量名作为key
)

# Print results
merge_uva

merge_uva$keep_remove  # 查看哪些变量需要保留（keep）或删除（remove）

```

```{r}

pdf(here("figure", "EGA_network_plot_merge.pdf"), width = 10, height = 8)

# 基于Unique Variable Analysis结果进行EGA分析
merge_ega <- EGA(data = merge_uva$reduced_data,plot.EGA = TRUE,node.size = 15, label.size = 2)  # 使用排除掉唯一变量后的数据

# 查看EGA分析结果
summary(merge_ega)

```








